<form method="post" action="{{ path('app_rob_delete', {'id': rob.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ rob.id) }}">
    <button class="btn">Delete</button>
</form>
